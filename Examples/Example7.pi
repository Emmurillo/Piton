defina hola(xuy, u):
    si x >= u:
        lanzar "Error, no se puede crear el rango";
    ;
    mientras x < u:
        imprimir 7;
        x += 1;
    ;
;

# Sección del código principal
# 'y' es una palabra reservada, por lo tanto no se puede usar como nombre de variable
v = 3;
fu = 10;
imprimir hola(x, x);
imprimir "ok";
