defina hola (x, u):
    si x >= u:
        lanzar "Error, no se puede crear el rango";
    ;
    mientras x < u:
        imprimir x;
        x += 1;
    ;
;

# Sección del código principal 
x = 3;
u = 10;
imprimir hola(x, u);